# Tiptap drag-handle statically imports yjs via its collab peers. Hoist yjs so a
# single copy resolves (avoids "multiple Yjs instances" crash). See docs/plans/2026-05-30-stack-decisions.md
public-hoist-pattern[]=*yjs*
public-hoist-pattern[]=*y-protocols*
